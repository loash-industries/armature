import { Card, CardHeader, CardContent } from "@awar.dev/ui";

export function DaoDashboard() {
  return (
    <Card>
      <CardHeader>Dashboard</CardHeader>
      <CardContent>DAO overview will go here.</CardContent>
    </Card>
  );
}
