# Stretch: Advanced Proposals

> Part of the [stretch features index](00_index.md). Not in hackathon scope.

---

| Type | Description |
|---|---|
| `SendSmallPayment<T>` | Rate-limited payments with daily/epoch caps via `ProposalTypeState` |
| `ProposeUpgrade` | Package upgrade authorization via stored `UpgradeCap` |

**See also:** [Proposal Composition](09_proposal_composition.md) for bundling multiple proposals into atomic composites, and [Open Proposal Type Set](11_open_proposal_type_set.md) for third-party proposal types.
